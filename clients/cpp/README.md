# C++ Courier Client

    > git submodule init --recursive

    > mkdir build
    > cd build
    > cmake .. -DCMAKE_INSTALL_PREFIX=../install
    > cd ..
    > cmake --build build -- -j
    > ./build/courier_test
    > make install
